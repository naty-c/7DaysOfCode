// 7 Days of Code D-1 | Alura + Rafaella Ballerini

let numberOne = 1
let stringOne = '1'
let numberThirty = 30
let stringThirty = '30'
let numberTen = 10
let stringTen = '10'

if (numberOne == stringOne && numberOne !== stringOne) {
    console.log('Variables numberOne and stringOne have the same value, but not the same type')
} else {
    console.log('Variables numberOne and stringOne have the same value')
}

if (numberThirty === stringThirty) {
    console.log('Variables numberThirty and stringThirty have the same value and also the same type')
} else {
  console.log('Variables numberThirty and stringThirty are not the same type')
}

if (numberTen == stringTen && numberTen !== stringTen) {
    console.log('Variables numberTen and stringTen have the same value, but not the same type')
} else {
    console.log('Variables numberTen and stringTen have the same value')
}
